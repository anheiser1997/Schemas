{
    "properties": {
        "rt": {
            "description": "The Resource Type.",
            "items": {
                "enum": ["x.com.comp.da.diagnosis"],
                "maxLength": 64,
                "type": "string"
            },
            "minItems": 1,
            "uniqueItems": true,
            "readOnly": true,
            "type": "array"
        },
        "if": {
            "description": "interface",
            "items": {
                "enum": ["oic.if.a", "oic.if.baseline"],
                "maxLength": 64,
                "type": "string"
            },
            "minItems": 1,
            "uniqueItems": true,
            "readOnly": true,
            "type": "array"
        },
        "x.com.comp.da.diagnosisStart": {
            "description": "Disgnosis Start",
            "type": "string",
            "enum": ["Run", "Ready"],
            "readOnly": false
        }
    },
    "type": "object"
}
